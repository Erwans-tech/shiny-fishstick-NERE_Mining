<section><p class="lead">{{ __('site.gallery_lead', [], $loc) }}</p>@if($media->isEmpty())<p class="lead">{{ __('site.gallery_empty', [], $loc) }}</p>@else<div class="gallery-grid">@foreach($media as $item)<figure class="gallery-item"><img src="{{ asset($item->file_path) }}" alt="{{ $item->title }}"><figcaption class="gallery-caption"><h3>{{ $item->title }}</h3>@if($item->caption)<p>{{ $item->caption }}</p>@endif</figcaption></figure>@endforeach</div>@endif</section>