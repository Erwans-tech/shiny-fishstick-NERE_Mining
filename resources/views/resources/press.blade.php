<section><p class="lead">{{ __('site.press_lead', [], $loc) }}</p><div class="grid-3">@forelse($documents as $document)<article class="card"><div class="card-tag">{{ $document->document_type }}</div><h3>{{ $document->title }}</h3>@if($document->description)<p>{{ $document->description }}</p>@endif@if($document->file_path)<a class="btn btn-gold" href="{{ asset($document->file_path) }}">{{ __('site.download_pdf', [], $loc) }}</a>@endif</article>@empty<p class="lead">{{ __('site.press_empty', [], $loc) }}</p>@endforelse</div></section>